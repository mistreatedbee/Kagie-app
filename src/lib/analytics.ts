export function trackEvent(name: string, payload?: any) {
  // Placeholder analytics hook. Integrate GA / Sentry here.
  // Example: window.gtag && window.gtag('event', name, payload)
  console.log('[analytics]', name, payload);
}
